# 
# project-x 2020
# 

desc = "禁止数据(联系人,短信,剪贴板,日历,定位,账户,电话...)"
author = ikas
version = 1

ops = [
  {name = READ_CONTACTS, mode = ignore, ruleMode = ignore}
  {name = WRITE_CONTACTS, mode = ignore, ruleMode = ignore}
  {name = READ_CALL_LOG, mode = ignore, ruleMode = ignore}
  {name = WRITE_CALL_LOG, mode = ignore, ruleMode = ignore}
  {name = READ_CALENDAR, mode = ignore, ruleMode = ignore}
  {name = WRITE_CALENDAR, mode = ignore, ruleMode = ignore}
  {name = WRITE_SMS, mode = ignore, ruleMode = ignore}
  {name = RECEIVE_MMS, mode = ignore, ruleMode = ignore}
  {name = RECEIVE_WAP_PUSH, mode = ignore, ruleMode = ignore}
  {name = SEND_SMS, mode = ignore, ruleMode = ignore}
  {name = READ_SMS, mode = ignore, ruleMode = ignore}
  {name = READ_CLIPBOARD, mode = ignore, ruleMode = ignore}
  {name = WRITE_CLIPBOARD, mode = ignore, ruleMode = ignore}
  {name = GET_ACCOUNTS, mode = ignore, ruleMode = ignore}
  {name = COARSE_LOCATION, mode = ignore, ruleMode = ignore}
  {name = READ_PHONE_STATE, mode = ignore, ruleMode = ignore}
  {name = READ_PHONE_NUMBERS, mode = ignore, ruleMode = ignore}
  {name = CAMERA, mode = ignore, ruleMode = ignore}
  {name = RECORD_AUDIO, mode = ignore, ruleMode = ignore}
  {name = READ_DEVICE_IDENTIFIERS, mode = ignore}
  {name = CHANGE_WIFI_STATE, mode = ignore}
  {name = GET_USAGE_STATS, mode = ignore}
]